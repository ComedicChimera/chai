package validate

import "chai/deps"

// ValidatePackage walks a package's expressions and functions, validates them
// (performing semantic analysis), and adds all the expressions to the HIR tree.
func ValidatePackage(pkg *deps.ChaiPackage) {

}
