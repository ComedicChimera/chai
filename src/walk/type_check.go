package walk

import "chai/typing"

// coerce applies a coercion constraint between two types. It returns a boolean
// indicating whether or not coercion succeeded (if it was even attempted).
func (w *Walker) coerce(from, to typing.DataType) bool {
	// TODO
	return false
}
