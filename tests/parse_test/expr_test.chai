def expr_multi_compare(a, b, c, d: i32) = a < b < c || b > d >= c < a

def expr_func(a, b: i32) i32 = a + b + 3

def expr_func_2(a, b, c: i32) i32 = a * c + b * 4 

def expr_func_3(a, b: f32) f32 = a < b || a * b == 4 && a - 2 * b >> 1 >= 5

def expr_func_simple(a: u64) u64 = a // 5

def expr_func_r_assoc(a, b: u32) = a ** b ** 2 - 5

def expr_func_parens(a, b: u8) u8 = (a + 5) * b - ()