@intrinsicop("iadd")
oper (+) (a, b: i32) i32 end

@intrinsicop("sconcat")
oper (+) (a, b: string) string end

@intrinsicop("imul")
oper (*) (a, b: i32) i32 end

def func_rt_type(x: i32) i32 = 3 + x

def func_1(a, b: i32, c: string) bool = true

def func_2(x, y: f32, z: u64) bool = true

def test_func() bool = func_2(3, 4.5, 67)

def block_func_1(a: i32) i32
    let x = a * 2
    (x + 4 + x)

    let y = x * 5, z = x + a

    (y + z * x)
end

def tuple_func() (i32, string) = (0, "")

def block_func_2(a, b: i32) 
    let x, y = tuple_func()

    let z = x + a * b

    z
end

def check_cast(a, b: i32, s: string)
    let c = a + b as u64

    (s as f32)
end

def assignment_test(a, b: i32) 
    let x = a + b

    x = 5

    5 = b
end